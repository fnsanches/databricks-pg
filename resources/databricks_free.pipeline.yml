# The main pipeline for databricks_free
resources:
  pipelines:
    databricks_free_pipeline:
      name: databricks_free_pipeline
      catalog: workspace
      schema: databricks_free_${bundle.target}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
