# This is a Databricks asset bundle definition for databricks_pg.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: databricks_pg
  uuid: 5000ad9c-1a82-49d6-b143-e58fe77e47c6

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-43481699-428a.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-43481699-428a.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/felinobre100@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/felinobre100@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: felinobre100@gmail.com
        level: CAN_MANAGE
